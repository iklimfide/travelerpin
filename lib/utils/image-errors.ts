/** Thrown when a file cannot be converted and must not be stored as-is (e.g. HEIC). */
export const UNSUPPORTED_IMAGE_FORMAT_ERROR = "UNSUPPORTED_IMAGE_FORMAT";

/** Pin photo picker / upload — file.size or materialized buffer exceeds LIMITS.maxPinPhotoBytes. */
export const PIN_PHOTO_FILE_TOO_LARGE_ERROR = "PIN_PHOTO_FILE_TOO_LARGE";
